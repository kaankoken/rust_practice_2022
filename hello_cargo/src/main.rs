/**
 * `cargo check` controls whether have error without generating binary files
 * `cargo build` builds the project
 * `cargo run` builds & run the project
 * `rustfmt filename.rs` formats the file
 */

fn main() {
    println!("Hello, world!");
}
